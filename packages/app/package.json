{
  "name": "app",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "start:app": "cross-env STATIC=../app/dist npm run start",
    "start:proto": "cross-env STATIC=../proto/public npm run start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx vite",
    "build": "npx tsc && npx vite build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@calpoly/mustang": "^1.0.14",
    "lit": "^3.2.1"
  }
}
